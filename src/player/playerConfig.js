// Player configuration settings

// Movement parameters
export const PLAYER_HEIGHT = 1.7; // Eye level height
export const PLAYER_SPEED = 40.0; // Movement speed factor
export const FRICTION = 10.0; // Deceleration factor
export const COLLISION_MARGIN = 0.5; // How close player can get to objects

// Health parameters
export const MAX_HEALTH = 100;
export const REGEN_RATE = 0.5; // Health regeneration per second when not taking damage
export const REGEN_DELAY = 5000; // Milliseconds before health regeneration starts after taking damage

// Weapon handling
export const HEADBOB_INTENSITY = 0.05;
export const HEADBOB_SPEED = 10.0;
